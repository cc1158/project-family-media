# 安全策略

## 部署边界

家映的家庭媒体服务面向可信家庭局域网使用，当前不提供公网身份认证。媒体浏览、原文件读取、扫描、缩略图重建和生成数据清理接口会对能够访问服务端口的设备开放。

- 不要把服务端端口直接映射到互联网。
- 远程访问应通过可信 VPN，或在独立反向代理层增加认证与 TLS。
- 不要在公开 Issue、日志或诊断信息中提交 NAS 地址、Token、用户名、设备 ID、媒体名称或家庭路径。
- Jellyfin Token 只应保存在 Apple Keychain，不应写入配置文件或 URL 日志。

## 报告安全问题

请不要在公开 Issue 中披露尚未修复的漏洞或家庭部署信息。使用 GitHub 仓库的私密漏洞报告功能联系维护者；如果该功能不可用，请先创建不包含敏感细节的普通联系请求。

## 支持范围

安全修复以当前基线及后续版本为准。历史候选版本可能不再单独维护。
